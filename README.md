# Order Generator Bot

Telegram бот для автоматической генерации заказов в прайс-листах поставщиков.

## Возможности

- 📋 Автоматическое сопоставление товаров по артикулам
- 📦 Объединение заказов на склад и предзаказов клиентов
- ⚙️ Настройка разметки прайс-листа для каждого поставщика
- 💾 Сохранение форматирования исходного файла (цвета, размеры ячеек, шрифты)
- 📁 Поддержка форматов .xls и .xlsx

## Установка и запуск

### Требования

- Docker и Docker Compose
- Токен Telegram бота (получить можно у [@BotFather](https://t.me/BotFather))

### Шаги установки

1. **Клонируйте репозиторий и перейдите в директорию проекта**

2. **Создайте файл `.env` на основе `.env.example`:**
   ```bash
   cp .env.example .env
   ```

3. **Укажите токен бота в файле `.env`:**
   ```
   BOT_TOKEN=your_bot_token_here
   ```

4. **Запустите бота через Docker Compose:**
   ```bash
   docker-compose up -d
   ```

5. **Просмотр логов:**
   ```bash
   docker-compose logs -f
   ```

6. **Остановка бота:**
   ```bash
   docker-compose down
   ```

## Использование

### Основные команды

- `/start` - Начать работу с ботом
- `/help` - Получить справку
- `/suppliers` - Управление поставщиками
- `/generate` - Сгенерировать заказ

### Процесс генерации заказа

1. Используйте `/suppliers` для добавления поставщика (если еще не добавлен)
2. Используйте `/generate` для запуска генерации заказа
3. Выберите поставщика из списка
4. Последовательно загрузите три файла:
   - **Прайс-лист производителя** - файл с прайс-листом, в котором нужно заполнить количество
   - **Заказ на склад** - файл с заказом на склад (структура соответствует прайс-листу)
   - **Предзаказы клиентов** - файл с предзаказами клиентов (список с датами)

5. Бот автоматически сгенерирует заказ и отправит вам готовый файл

### Настройка поставщиков

Для каждого поставщика можно настроить разметку прайс-листа:

- **Столбец с артикулом** - номер столбца, где находится артикул товара (начиная с 0)
- **Столбец для количества** - номер столбца, куда записывать количество заказа
- **Строка начала данных** - номер строки, с которой начинаются данные товаров (начиная с 0)

По умолчанию используется следующая конфигурация:
- Прайс-лист: артикул в столбце 0, количество в столбце 9, данные начинаются со строки 2
- Заказ на склад: артикул в столбце 0, количество в столбце 9, данные начинаются со строки 2
- Предзаказы: первый артикул в столбце 2, второй артикул в столбце 5, количество в столбце 4, данные начинаются со строки 2

## Структура проекта

```
.
├── bot.py                 # Основной файл Telegram бота
├── excel_processor.py     # Модуль для работы с Excel файлами
├── order_generator.py     # Модуль генерации заказов
├── config_manager.py      # Управление конфигурациями поставщиков
├── test_order.py          # Тестовый скрипт
├── requirements.txt       # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
├── .env.example           # Пример файла с переменными окружения
├── suppliers_config.json  # Файл с конфигурациями поставщиков (создается автоматически)
├── uploads/               # Директория для загруженных файлов
└── outputs/               # Директория для сгенерированных заказов
```

## Логика генерации заказа

1. Бот читает прайс-лист производителя и извлекает список товаров с артикулами
2. Бот читает заказ на склад и извлекает артикулы и количества
3. Бот читает предзаказы клиентов и извлекает артикулы и количества
4. Для каждого артикула из прайс-листа:
   - Если есть количество в заказе на склад И в предзаказах → складываем их
   - Если есть только в одном источнике → берем это количество
   - Если нет ни в одном источнике → оставляем поле пустым
5. Записывает итоговые количества в прайс-лист с сохранением форматирования

## Разработка

### Локальный запуск без Docker

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Создайте файл `.env` с токеном бота

3. Запустите бота:
   ```bash
   python bot.py
   ```

### Тестирование

Для тестирования на примерах файлов:
```bash
python test_order.py
```

## Поддержка

При возникновении проблем проверьте:
- Правильность токена бота в `.env`
- Наличие всех необходимых файлов в директории `examples/`
- Логи бота через `docker-compose logs`

